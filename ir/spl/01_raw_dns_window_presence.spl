index=dns_soc_dns host=dns-soc-resolver01 sourcetype=unbound:dns earliest=1787726220 latest=1787726520
| stats count as raw_events min(_time) as first_event max(_time) as last_event
| convert ctime(first_event) ctime(last_event)
